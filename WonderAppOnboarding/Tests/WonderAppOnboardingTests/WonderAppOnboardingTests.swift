@testable import WonderAppOnboarding
import XCTest

final class WonderAppOnboardingTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(WonderAppOnboarding().text, "Hello, World!")
    }
}
